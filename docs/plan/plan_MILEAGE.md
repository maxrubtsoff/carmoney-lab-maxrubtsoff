# План MILEAGE — правило «пробег ≤ 400 000 км, иначе review»

Задача: в расчёт решения по заявке добавить правило — если пробег авто больше
400 000 км, итоговое решение должно быть `review` (даже если по LTV было бы
`approve` или `reject`). Ниже — план реализации. Код в рамках этой задачи не
меняется.

Контекст (свежо с `docs/setup/code_map.md` и чтения файлов):

- Решение выбирается в `backend/src/Domain/DecisionEngine.php::decide(float $ltv)`
  единственно по LTV; пробег на решение сейчас не влияет.
- Пробег проверяется только в валидаторе
  (`backend/src/Domain/ApplicationValidator.php:43-46`) на диапазон
  0…`rules.vehicle.max_mileage_km` (= 500 000) — это валидация, на 422 она
  завершается без решения.
- В `backend/config/rules.php` порога 400 000 нет, есть только
  `vehicle.max_mileage_km = 500000` (другая сущность, валидационный потолок).
- Сборка оркестратора — `backend/src/AppFactory.php:30-39`. Тесты — `tests/Unit/DecisionEngineTest.php`,
  `tests/Unit/AssessmentServiceTest.php`.

Точка вставки (по коду): порог в `rules.php`; проверка в `DecisionEngine` после
текущих веток по LTV; проброс пробега из `AssessmentService::assess()` в
`decide()`. Это «вариант A» из code_map.md, п. 2.

## Файлы

Что и где меняется (по строке на файл):

- `backend/config/rules.php` — добавить ключ (новая, после блока `vehicle`,
  строки 20–24, например новая строка ~25). Имя ключа обсуждается с заказчиком
  (см. вопросы).
- `backend/src/Domain/DecisionEngine.php` — строка 20-21 (новое свойство
  порога), строка 23 (doc-блок), строка 24-28 (конструктор читает порог),
  строка 30 (сигнатура `decide(float $ltv, int $mileage): string`), строки
  32-41 (после текущих if — финальное `if ($mileage > $threshold) return REVIEW;`).
- `backend/src/Domain/AssessmentService.php` — строка 33 (`decide($ltv)` →
  `decide($ltv, (int) $input['mileage'])`).
- `backend/src/AppFactory.php` — строка 37 (передача новой секции в
  `DecisionEngine` конструктор).
- `tests/Unit/DecisionEngineTest.php` — строка 17 (setUp создаёт `DecisionEngine`
  с порогом пробега), строка 23 (вызов `decide($ltv, $mileage)`), строки 27-37
  (DataProvider — текущие строки по LTV оставить как есть, добавить новый
  DataProvider `mileageOverrides`).
- `tests/Unit/AssessmentServiceTest.php` — без структурных правок: используется
  готовый payload из `payload()` (строка 33-43, mileage=96000), под граничные
  случаи добавить 3 новых теста (см. раздел Тесты).
- `docs/setup/code_map.md` — не правим в этой задаче (агентская инструкция
  разойдётся с реальностью уже после фикса; обновит следующий проход по карте).
- `docs/spec/` и `docs/intent/` — не правим: новая фича пришла задачей, не
  спецификацией.

## Шаги реализации

По порядку:

1. Добавить в `backend/config/rules.php` новый ключ с порогом 400 000 (см.
   «Вопрос 1» — точное имя ключа и где разместить).
2. Расширить конструктор `DecisionEngine`: принимать порог пробега (int)
   дополнительно к LTV-порогам; сохранить в приватное свойство; обновить doc-блок
   класса (комментарий над классом описывает только LTV — добавить строку про
   пробег).
3. Изменить сигнатуру `decide(float $ltv, int $mileage): string`. Сохранить
   текущее поведение по LTV без изменений (порядок проверок: LTV → пробег),
   затем в самом конце — `if ($mileage > $this->mileageThreshold) return REVIEW;`.
   Так порог пробега только понижает решение (см. «Вопрос 2» про
   reject→review).
4. В `AssessmentService::assess()` (строка 33) пробросить `(int) $input['mileage']`
   вторым аргументом. `$input['mileage']` уже приходит из валидатора как int.
5. В `AppFactory::create()` (строка 37) передать порог пробега в `DecisionEngine`
   из `$rules` (новая секция или ключ, см. вопрос 1).
6. Обновить `tests/Unit/DecisionEngineTest.php`: setUp создаёт движок с порогом
   пробега 400 000; текущий `testDecidesByLtv` адаптировать — вызывать
   `decide($ltv, 0)` (пробег ниже порога не влияет); добавить новый DataProvider
   `mileageOverrides` и тест `testMileageAboveThresholdForcesReview`.
7. Добавить в `tests/Unit/AssessmentServiceTest.php` три новых теста на
   пограничные значения (см. раздел Тесты). LTV во всех трёх — в зелёной зоне
   (≈50.0), чтобы без нового правила было бы `approve`, а с правилом —
   `review`.
8. Прогнать `make lint` и `make test`. Не править существующие ожидания
   тестов ради зелёного прогона (по AGENTS.md п.5).
9. Прогнать `make up` и проверить `POST /api/applications` вручную на пробеге
   400 001 км с LTV ≈ 50 — ожидаем `decision: "review"`.

## Тесты

Граничные значения (отдельной строкой, как требует инструкция):

- пробег `399999` — ожидание `approve` (LTV в зелёной зоне; ниже порога).
- пробег `400000` — ожидание `approve` (на самой границе; правило
  «больше 400 000», не «больше или равно» — это закреплено в задаче).
- пробег `400001` — ожидание `review` (выше порога на 1).
- пробег пустой/не передан — отдельно: валидатор сейчас трактует отсутствие
  поля как `-1` и бросает `ValidationException` → HTTP 422 (см. «Вопрос 3»).
  Поведение не меняем: «пустой пробег» — это всё ещё валидационная 422, а не
  `review`.

Дополнительные кейсы (покрытие взаимодействия правил):

- `DecisionEngineTest::mileageOverrides` — DataProvider:
  - LTV ≈ 50 (approve-зона), пробег 400001 → `review` (правило понижает approve).
  - LTV = 85.0 (review-зона), пробег 400001 → `review` (без изменений).
  - LTV = 95.0 (reject-зона), пробег 400001 → `review` (правило перезаписывает
    reject — см. «Вопрос 2», если заказчик выберет «только понижать», ожидание
    станет `reject`).
  - LTV ≈ 50, пробег 400000 → `approve` (граница не включена).
- Существующие тесты `DecisionEngineTest::ltvValues` адаптировать вызовом
  `decide($ltv, 0)` — 0 км ниже 400 000, не влияет, ожидания прежние.
- `AssessmentServiceTest` — три теста: `testApprovesAtMileageBelowThreshold`
  (пробег 399999, LTV 50), `testApprovesAtMileageBoundary` (пробег 400000,
  LTV 50), `testReviewsAtMileageAboveThreshold` (пробег 400001, LTV 50).
  Для пробега 400001 дополнительно `approved_limit === 0` (как у любого
  не-approve).
- Регрессия валидации: существующий тест `ApplicationValidatorTest` на
  пробег > 500000 остаётся зелёным без правок.

## Риски

Что может сломаться / требует внимания:

- Существующая проверка `max_mileage_km = 500000` остаётся в `ApplicationValidator`
  без изменений. Новый порог 400 000 — **отдельный** ключ, он не должен
  затронуть валидационный потолок 500 000. Если случайно завязать одно на
  другое, валидация начнёт резать заявки на пробеге 400 001 как 422 — это
  не то поведение, которое просит задача.
- `DecisionEngine::decide()` сейчас — единственный потребитель `ltv`.
  Расширение сигнатуры требует синхронного изменения всех вызывающих сторон:
  `AssessmentService::assess()` (строка 33) и `DecisionEngineTest` (строка 23).
  Если сигнатуру расширить, а вызовы забыть — PHPUnit провалится.
- Существующий тест `DecisionEngineTest::testDecidesByLtv` (строка 20-24)
  сейчас вызывает `decide($ltv)` — с новой сигнатурой он упадёт на
  ArgumentCountError. Не «чинить» правкой ожиданий: корректно — добавить
  второй аргумент в вызов.
- Тест `AssessmentServiceTest::testApprovesLowLtvAndSetsLimitToRequestedAmount`
  использует `mileage = 96000` (строка 38) — это ниже порога 400 000, на
  approve-зону не повлияет. Если кто-то «оптимизирует» payload() и поднимет
  mileage выше — тест молча сменит поведение. Не трогать payload() без
  явной причины.
- Правило `mileage > threshold` пишем именно как строгое `>`, чтобы граница
  400 000 осталась approve. Если написать `>=` — граница станет review, и
  тест `testApprovesAtMileageBoundary` провалится. Это противоречит формулировке
  задачи («не больше 400 000»).
- AGENTS.md п.5: пороги и формулы в `rules.php` и ожидания тестов не меняются
  ради зелёного `make test`. Если риск-менеджмент скажет «порог 350 000» —
  остановиться и уточнить у человека.
- Что **не входит** в эту задачу и не делается без отдельной задачи:
  - Не меняем `ApplicationValidator` (валидация ≠ решение).
  - Не трогаем `LtvCalculator`, `VehicleAge`, `VinValidator`.
  - Не заводим поле `mileage` в БД-миграцию (оно уже есть в `vehicles.mileage_km`,
    см. code_map.md п. 3).
  - Не правим `frontend/` (форма уже шлёт `mileage`).
  - Не делаем «только понижение approve→review» против «перезапись любого
    решения» — это вопрос к заказчику (см. ниже).
  - Не меняем существующее поведение на границе LTV 60.0 (строгое `<` vs
    `<=` — известное расхождение между кодом и doc-блоком, не наша задача).

## Вопросы, на которые без заказчика не ответить

1. Имя и место нового ключа в `backend/config/rules.php`. Варианты:
   `vehicle.mileage_review_above_km` (рядом с `max_mileage_km`),
   `decision.mileage_review_above_km` (отдельная секция под пороги решения),
   `vehicle.high_mileage_review_km`. AGENTS.md п.5: это бизнес-число, без
   согласования риск-менеджмента править `rules.php` нельзя.
2. Семантика правила при `reject`: «пробег > 400 000 перезаписывает любое
   решение в `review`» (как в коде code_map.md вариант A, в т.ч. reject →
   review — смягчение) или «правило только понижает approve→review» (reject
   остаётся reject). Это меняет один из кейсов DataProvider.
3. Граница «включительно/исключительно» на 400 000. Задача говорит «не больше
   400 000», то есть 400 000 — ещё ок. В коде пишем `>`, проверки
   `testApprovesAtMileageBoundary` именно на это завязаны. Подтверждение нужно,
   потому что в задачах по LTV границы трактовались иначе (строгое `<` в коде
   при `LTV <= approve_max -> approve` в доке — code_map.md п. 1 ш. 4).
4. Поведение при отсутствии поля `mileage` в payload: оставляем текущее
   (422 через валидатор) или новое правило должно как-то это обработать
   отдельно? Сейчас план исходит из «оставляем как есть», но задача
   явно просит пустой пробег в тестах — нужно понимать, чего ждёт заказчик:
   «пустой пробег = 422» или «пустой пробег = review».
5. Нужно ли сохранять порог 400 000 рядом с `max_mileage_km = 500000` (тогда
   неочевидно, зачем два порога по пробегу) или вынести в отдельную секцию
   `decision`. Затрагивает только читаемость конфига, но п.5 AGENTS.md просит
   не менять правила без согласования.
## ★ Сравнение со встроенным агентом plan

Встроенный `plan` (модель GLM 5.3) по правам из `kilo.jsonc` не смог писать в
`docs/plan/` (там разрешён только `.kilo/plans/**.md`) — сам это заметил и сохранил
свой план в `.kilo/plans/1790259494835-mileage-review-rule.md`, честно указав в
ответе, что путь `docs/plan/plan_MILEAGE.md` был отклонён правами.
По содержанию пришёл к тем же выводам: граница строго `>` 400000 (400000 →
approve, 400001 → review), правило только понижает approve→review (reject не
смягчает), пустой пробег остаётся ошибкой валидации (422) — семантика совпадает
с нашим планом.
Отличие в подаче: встроенный `plan` заканчивается интерактивным вопросом «Готовы
реализовать?» с вариантами (новая сессия / продолжить здесь / уточнить), наш
`planner` — просто markdown-файл без запроса на дальнейшее действие.
